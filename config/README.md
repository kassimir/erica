# config

- **`persona.yaml`** — Erica name, tone, modes, constraints, example responses (loaded by the agent).
- **`workflows/`** — YAML workflow definitions (`steps`, `triggers`, `required_skills`).

See [`../ERICA_ARCHITECTURE.md`](../ERICA_ARCHITECTURE.md) for how these integrate with the agent.
