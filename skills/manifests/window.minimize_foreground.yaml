name: window.minimize_foreground
description: Minimize the foreground window.
entrypoint: skills.window_skill:minimize_foreground
parameters: []
permissions:
  - window.manage
examples:
  - minimize the window
